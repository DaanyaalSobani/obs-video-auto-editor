version: '3.8'

services:
  video-editor:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./uploads:/app/uploads
    environment:
      - PORT=3000
      - DEMO_MODE=${DEMO_MODE:-false}
    restart: unless-stopped
